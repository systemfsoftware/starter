name: Force Release

on:
  workflow_dispatch:
    inputs:
      bump:
        description: "Release bump for @TODO/starter (TODO: your package name)"
        required: true
        default: patch
        type: choice
        options:
          - patch
          - minor
          - major

permissions:
  contents: write
  pull-requests: write

jobs:
  force-release:
    name: dummy changeset + release PR
    runs-on: ubuntu-latest
    env:
      HUSKY: "0"
    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0
      - name: Guard · refuse a workspace with no publishable package
        run: |
          if [ "$(jq -r 'select(.private != true) | .name' packages/*/package.json | grep -c .)" -eq 0 ]; then
            echo "::error::every packages/*/package.json is private; drop \"private\": true and rename the placeholder before forcing a release"
            exit 1
          fi
      - name: Write dummy changeset
        run: |
          cat > ".changeset/force-${{ github.run_id }}-${{ inputs.bump }}.md" <<EOF
          ---
          "@TODO/starter": ${{ inputs.bump }}
          ---

          chore(release): force ${{ inputs.bump }} release via workflow dispatch
          EOF
      - name: Commit, push, and open PR
        env:
          GH_TOKEN: ${{ github.token }}
          BRANCH: force-release/${{ inputs.bump }}-${{ github.run_id }}
          BASE: ${{ github.event.repository.default_branch }}
          BUMP: ${{ inputs.bump }}
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git switch --create "$BRANCH"
          git add .changeset
          git commit -m "chore(release): force $BUMP via workflow dispatch"
          git push origin "$BRANCH"
          gh pr create \
            --base "$BASE" \
            --head "$BRANCH" \
            --title "chore(release): force $BUMP" \
            --body "Manually triggered ${BUMP} release (run ${{ github.run_id }}). Merging drops a dummy changeset intent on main; the Release workflow then opens the version-packages PR."
